<h1>My Tasks</h1>

<% @tasks.each do |task| %>
<ul><%= link_to "#{task.title}", "/tasks/#{task.id}" %> </ul>
<ul> <%= link_to "Destroy", task_path(task.id), data: {:confirm => "Are you sure?"}, :method => :delete %> </ul>

<% end %>
